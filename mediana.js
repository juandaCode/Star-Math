"use strict";

function esPar(numerito) {
    if(numerito % 2 == 0) {
        return true;
    }
    else {
        return false;
    }
}

if(esPar(lista.length)) {

}
else {
    const mitadLista = lista.length / 2;
}